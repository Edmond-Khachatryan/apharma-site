# 🔐 Решение проблемы верификации GitHub

## ⚠️ **Ошибка:** "Your account requires further verification"

### 📋 **Причины:**
1. **Новый аккаунт** — GitHub просит подтвердить email/телефон
2. **Подозрительная активность** — много коммитов за короткое время
3. **Отсутствие 2FA** — GitHub может требовать включить двухфакторную аутентификацию

### ✅ **Решения:**

#### **Вариант 1: Проверить Email**
1. Откройте https://github.com
2. Зайдите в **Settings** → **Emails**
3. Проверьте, что email подтверждён (зелёная галочка ✅)
4. Если нет — нажмите "Resend verification email"
5. Проверьте почту и нажмите на ссылку

#### **Вариант 2: Включить 2FA**
1. Зайдите в **Settings** → **Password and authentication**
2. Раздел **Two-factor authentication**
3. Нажмите **Enable two-factor authentication**
4. Следуйте инструкциям (Telegram/Authy)

#### **Вариант 3: Проверить SSH ключи**
Если используете SSH:

```bash
# Проверить ключ
ssh -T git@github.com

# Если не работает, добавить новый ключ
ssh-keygen -t ed25519 -C "your_email@example.com"
cat ~/.ssh/id_ed25519.pub

# Скопируйте вывод и добавьте на:
# https://github.com/settings/keys
```

#### **Вариант 4: Использовать Personal Access Token**
1. Зайдите в **Settings** → **Developer settings** → **Personal access tokens** → **Tokens (classic)**
2. Нажмите **Generate new token**
3. Выберите срок действия (например, 90 дней)
4. Отметьте чекбоксы:
   - ✅ `repo` (полный доступ к репозиториям)
   - ✅ `workflow` (для GitHub Actions, если используете)
5. Скопируйте токен (показывается один раз!)
6. Используйте токен вместо пароля при `git push`

**Важно:** Токен = пароль! Никому не показывайте!

### 🚀 **Быстрое решение (для вашего проекта):**

1. **Откройте Git Bash** в папке проекта
2. Попробуйте снова:

```bash
git add .
git commit -m "Your message"
git push
```

3. Если попросит логин/пароль:
   - **Username:** ваш GitHub username
   - **Password:** используйте Personal Access Token (см. выше)

### 📝 **Альтернатива: Через браузер**

Если ничего не помогает, можете работать через веб-интерфейс:

1. Откройте https://github.com/Edmond-Khachatryan/apharma-site
2. Используйте веб-редактор для быстрых правок
3. Коммитьте прямо через браузер

### ✅ **После решения:**

Когда GitHub будет доволен:
- Все ваши правки уже в репозитории
- Git будет работать нормально
- Vercel подтянет последние изменения автоматически

---

**💡 Совет:** После включения 2FA GitHub перестанет постоянно просить подтверждения!

